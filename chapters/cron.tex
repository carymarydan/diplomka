\label{sec:cron}
Aby bylo možné automatizovat sklízení dat, je k tomu využit nástroj Cron\footnote{Cron je softwarový démon, který v operaèních systémech automatizovanì spouští v urèitý èas nìjaký pøíkaz resp. proces (skript, program apod.). Jedná se vlastnì o specializovaný systémový proces, který v operaèním systému slouží jakožto plánovaè úloh, jenž umožòuje opakované spouštìní periodicky se opakujících procesù (napø. noèní bìhy dávkových úloh pøi hromadném zpracování dat) apod.\cite{wiki.cron}}. Tento linuxový nástroj v pravidelných intervalech spouští skript, který celý proces automatického sklízení dat pøes OAI-PMH obsluhuje. V NTK je tímto souborem \texttt{oai-import-cron.sh}, který dále spouští skripty na restartování systému VuFind, sklizení dat, import dat, pøípadnì vymazání dat a optimalizaci indexu Solr. \cite{cron}
