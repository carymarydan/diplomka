\label{sec:firewall}
V dnešní dobì je na zabezpeèení ve stále se rozvíjejícím virtuálním svìtì kladen èím dál vìtší dùraz. To proto, e se v tomto prostoru odehrávají èinnosti èím dál dùleitìjší pro bìnı ivot. Zaopatøení proti kybernetickım útokùm je pro jakoukoli firmu nezbytné. Nejinak tomu je i v NTK, kde jedním z nástrojù pro zabezpeèení síového provozu je brána firewall. Tato standardní brána firewall je v základním vybavení operaèních systémù serverù a její ovládání je v zásadì jednoduché.

Pro systém VuFind je potøeba provést v nastavení firewall urèité zmìny. Konkrétnì se jedná o povolení komunikace na portech, které VuFind pouívá. Pro dostupnost katalogu ve veøejné síti internet se pouívá bìnı port pro pøenos HTTP protokolu, jen má èíslo 80. Další pouívané porty jsou 8080 a 8181, na kterıch probíhá komunikace VuFindu s vyhledávacím strojem Solr. Pøíkaz na povolení portu 80 vypadá takto:
\begin{verbatim}
firewall-cmd --zone=public --add-port=80/tcp --permanent
\end{verbatim}
\cite{firewall.cmd}

Dalším zabezpeèením VuFindu je šifrování komunikace pøes zabezpeèenı protokol HTTPS, která probíhá na portu 443. V nastavení webového serveru Apache je nakonfigurováno automatické pøesmìrování z protokolu HTTP na HTTPS. Aby server mohl pouívat takové zabezpeèení musí bıt nejprve ovìøen autoritou a získat certifikát. Pro NTK je touto autoritou spoleènost Cesnet, která ovìøuje její sluby v síti internet a vydává certifikáty, které NTK úspìšnì získává.
\cite{cesnet}

