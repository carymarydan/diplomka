\label{sec:instalace}
Nejprve je nutné aktualizovat operaèní systém serveru, kterım je v tomto pøípadì linuxová distribuce Red Hat Enterprise Linux Server release 7.1 (Maipo). O to se postará pøíkaz: \begin{verbatim}yum update
\end{verbatim}
Dále je nutné mít server vybaven nezbytnımi komponentami jako jsou webovı server, databázovı systém, php interpretr vèetnì nìkolika jeho modulù a v poslední øadì java prostøedí. K pouití jsou následující pøíkazy:
\begin{verbatim}
yum install httpd
yum install mysql-server
yum install php php-devel php-intl php-ldap 
yum install php-mysql php-xsl php-gd php-mbstring php-mcrypt
yum install java-*-openjdk-devel
\end{verbatim}
Nyní pøichází na øadu staení samotného systému VuFind. To je moné provést z úloištì \url{https://sourceforge.net/projects/vufind/files/VuFind/}, kde jsou k dispozici všechny verze VuFindu od jeho vzniku a po souèasnost. V tomto pøípadì se jedná o  staení verze 2.3.1, pøíkazem:
\begin{lstlisting}
wget http://downloads.sourceforge.net/vufind/vufind-2.3.1.tar.gz?use_mirror=osdn -O vufind-2.3.1.tar.gz
\end{lstlisting}
Po rozbalení staeného archivu, spustíme instalaci VuFindu pøíkazem:
\begin{verbatim}
php install.php
\end{verbatim}
Systém je nainstalován. Nyní je ještì potøeba provést nìkterá nezbytná nastavení pro správnı chod systému. Musí se dát vìdìt webovému serveru Apache o našem novì nainstalovaném VuFindu. K tomu slouí konfiguraèní soubor \texttt{httpd-vufind.conf}, kterı se nachází v adresáøi \texttt{local}. Apache standardnì naèítá konfiguraèní soubory ze svého umístìní, kterım je:
\begin{verbatim}
/etc/httpd/conf.d
\end{verbatim}
kam konfiguraèní soubor pro VuFind zkopírujeme. Alternativním øešením je pouití symbolického linku z konfiguraèního prostøedí Apache na konfiguraèní soubor VuFindu v jeho pùvodním umístìní.
Dále je nastaveno síové zabezpeèení, tzn. firewall. Ten zamezuje neádoucím pøístupùm na server. Aby však bylo moné k VuFindu pøistupovat, resp. byl dostupnı ze sítì internet, v nastavení firewall se povoluje port 80, kterı slouí právì k pøenosu http protokolu. Provede se pøíkazem:
\begin{verbatim}
firewall-cmd --zone=public --add-port=80/tcp --permanent
\end{verbatim}
V posledním kroku se pøepne zabezpeèení rozšíøeného jádra operaèního systému, tzv. Security-Enhanced, do permisivního módu, pøíkazem:
\begin{verbatim}
setenforce 0
\end{verbatim}
Kdy je VuFind úspìšnì nainstalován a okolní prostøedí správnì nastaveno, zapíná se v koøenovém adresáøi spouštìcím skriptem s parametrem \texttt{start} takto:
\begin{verbatim}
./vufind.sh start
\end{verbatim}
V tuto chvíli se v otevøeném prohlíeèi po zadání pøíslušné URL zobrazí úvodní stránka novì nainstalovaného portálu VuFind.


Nyní nastává fáze tzv. automatické konfigurace, která probíhá na URL adrese VuFindu následované øetezcem \texttt{/Install/Home}, kde vidíme seznam 7 poloek, viz. obrázek \ref{fig:auto-conf}. Poloky jsou barevnì rozlišeny podle toho, zda je daná oblast nastavena správnì (zelená barva) èi nikoli (barva èervená). Toto je tøeba zkontrolovat a popøípadì opravit problematické oblasti kliknutím na tlaèítko \uv{Fix}. Toková akce provede opravu nastavení dané oblasti buï automaticky, nebo uvede na obrazovku návod, jak vyøešit problém manuálnì, popøípadì se spustí prùvodce nastavením.

\begin{figure}[h]
\includegraphics[width=0.6\textwidth]{auto-conf}
\centering
\caption{Ukázka automatické konfigurace VuFindu v Národní technické knihovnì.}
\label{fig:auto-conf}
\end{figure}

V tuto chvíli je VuFind pøipraven k implementaci konkrétních potøeb knihovny a pøizpùsobení danému prostøedí. \cite{vufind.org}

