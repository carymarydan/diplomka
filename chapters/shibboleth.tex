\label{sec:shibboleth}
V Národní technické knihovnì je proces autentikace a autorizece uživatelù øešen pomocí technologie Shibboleth. Další zpùsoby, jak toto øešit ve VuFindu, jsou pomocí LDAP, ILS, databáze, SIP2, CAS, MultiAuth, ChoiceAuth, MultiILS a nebo Facebooku. V již zmínìném konfiguraèním souboru \texttt{config.ini} je tøeba provést patøièné nastavení Shibbolethu. \cite{vufind.auth}

Nastavení autentizace Shibboleth v portálu VuFind Národní technické knihovny vypadá takto:
\begin{verbatim}
username           	= uid
login              	= https://vufind.techlib.cz/Shibboleth.sso/Login
logout             	= https://vufind.techlib.cz/Shibboleth.sso/Logout
target             	= https://vufind.techlib.cz/MyResearch/Home
provider_id        	= https://shibboleth.techlib.cz/idp/shibboleth
cat_username       	= accountID
mail               	= HTTP_MAIL
\end{verbatim}

Shibboleth je volnì dostupný open-source middleware pro federativní autentikaci a autorizaci (AAI). S využitím protokolu Security Assertion Markup Language (SAML) poskytuje technický rámec pro federativní AAI. Shibboleth je projektem konzorcia Internet2.
\cite{shib.zdroj}
